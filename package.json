{
  "name": "hermes-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "A Tailscale-only installable mobile control surface for Hermes Agent.",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test 'test/*.test.mjs'",
    "format": "prettier --write 'public/*.{js,css}' server.mjs 'test/*.mjs'",
    "check": "node --check server.mjs && node --check public/app.js && prettier --check 'public/*.{js,css}' server.mjs 'test/*.mjs'"
  },
  "dependencies": {
    "http-proxy": "^1.18.1"
  },
  "devDependencies": {
    "prettier": "^3.9.6"
  }
}
