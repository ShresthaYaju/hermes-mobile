{
  "name": "hermes-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "A Tailscale-only installable mobile control surface for Hermes Agent.",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test 'test/*.test.mjs'",
    "format": "prettier --write 'public/**/*.{js,css}' '*.mjs' 'test/*.mjs'",
    "check": "for f in *.mjs public/*.js public/lib/*.js public/views/*.js; do node --check \"$f\" || exit 1; done && prettier --check 'public/**/*.{js,css}' '*.mjs' 'test/*.mjs'"
  },
  "dependencies": {
    "http-proxy": "^1.18.1",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "prettier": "^3.9.6"
  }
}
